# 智能油库日报机器人

每日自动从数据库读取设备库存与同步状态，生成格式化 Excel 报表并推送至企业微信群。

---

## 功能概述

- 从 MySQL 数据库一次查询获取所有设备信息（库存百分比、在线状态、安装信息等）
- 根据 `modify_time` 距今时长及设备 `status` 字段，准确判断设备网络是否在线
- 生成带条件格式（库存红绿灯 + 网络状态高亮）、微软雅黑字体、交替行底色、冻结首行、自动筛选的 Excel 报表
- 通过企业微信 Webhook 将报表文件推送到指定群
- 支持**飞书文档**（API）/ **腾讯文档**（自动转换）/ **本地 Excel** 三渠道加载设备配置
- 支持"**未录入系统设备**"补录：设备已交付但未入库时，可在配置表中手动补录，自动追加到报表末尾
- 支持**飞书告警推送**：程序停止或出现非致命异常时，主动推送告警消息至指定飞书机器人
- 在线配置加载成功后，自动与本地配置对比设备数量，差异 ≥ 5 台时推送告警
- 每日 08:00 定时自动执行

---

## 目录结构

```
Daily_Report_Robot/
├── main.py                # 主程序
├── device_config.xlsx     # 本地设备配置（在线表格不可用时的备用）
├── requirements.txt       # Python 依赖清单
├── deploy_linux.sh        # Linux 一键部署脚本（systemd 服务）
├── deploy_windows.bat     # Windows 一键部署脚本（NSSM 服务）
└── README.md
```

---

## 环境依赖

Python 3.8+，安装依赖：

```bash
pip install -r requirements.txt
```

---

## 配置说明

编辑 [main.py](main.py) 顶部的配置区域，**所有配置均支持通过环境变量注入，优先级高于代码中的默认值**：

### 1. 数据库配置

```python
DB_CONFIG = {
    'host':     os.environ.get('DB_HOST',     '数据库IP'),
    'port':     int(os.environ.get('DB_PORT', '3306')),
    'user':     os.environ.get('DB_USER',     '用户名'),
    'password': os.environ.get('DB_PASSWORD', '密码'),
    'db':       os.environ.get('DB_NAME',     'oil'),
    'charset':  'utf8mb4'
}
```

### 2. 企业微信机器人 Webhook

```python
WEBHOOK_URL = os.environ.get('WEBHOOK_URL',
    "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY")
```

### 3. 在线配置文件

```python
# 填入分享链接即可，程序自动识别平台
CONFIG_EXCEL_URL = os.environ.get('CONFIG_EXCEL_URL', "https://...")

# 飞书文档专用 API 凭据（其他平台留空）
FEISHU_APP_ID     = os.environ.get('FEISHU_APP_ID',     '')
FEISHU_APP_SECRET = os.environ.get('FEISHU_APP_SECRET', '')

# 本地备用配置文件（在线表格不可用时自动回退）
CONFIG_LOCAL_FILE = os.environ.get('CONFIG_LOCAL_FILE', 'device_config.xlsx')
```

### 4. 飞书告警 Webhook

```python
# 用于接收程序异常告警的飞书机器人 Webhook 地址
# 留空则不推送告警（静默失败，不影响主流程）
FEISHU_ALERT_WEBHOOK = os.environ.get('FEISHU_ALERT_WEBHOOK', '')
```

**告警触发场景：**

| 级别 | 触发场景 | 消息前缀 |
|------|----------|----------|
| 致命（fatal） | 程序被 Ctrl+C 或系统信号中断 | `[服务停止]` |
| 致命（fatal） | 程序主循环抛出未捕获异常 | `[服务停止]` |
| 警告（warning） | 在线配置缺少必需 Sheet，已切换本地文件 | `[运行警告]` |
| 警告（warning） | 在线配置无有效数据，已切换本地文件 | `[运行警告]` |
| 警告（warning） | 本地配置文件读取失败 | `[运行警告]` |
| 警告（warning） | 在线与本地设备编号数量差异 ≥ 5 台 | `[运行警告]` |
| 警告（warning） | 设备配置 Sheet：设备编号为空 | `[运行警告]` |
| 警告（warning） | 设备配置 Sheet：非补录设备桶数或设备归属为空 | `[运行警告]` |
| 警告（warning） | 设备配置 Sheet：非补录设备编号在数据库中无匹配 | `[运行警告]` |
| 警告（warning） | 设备配置 Sheet：总数核对不符（配置数 ≠ 数据库设备数 + 实际补录数） | `[运行警告]` |
| 警告（warning） | 排除客户设置 Sheet：排除客户ID为空或重复 | `[运行警告]` |
| 警告（warning） | 排除客户设置 Sheet：排除规则在数据库中无匹配设备 | `[运行警告]` |
| 警告（warning） | 计量客户设置 Sheet：计量客户ID为空或重复 | `[运行警告]` |
| 警告（warning） | 计量客户设置 Sheet：计量客户ID在数据库中无匹配设备 | `[运行警告]` |
| 警告（warning） | 未录入系统设备 Sheet：客户名称或设备编号为空，或设备编号重复 | `[运行警告]` |
| 警告（warning） | 补录设备已入库，需从"未录入系统设备" Sheet 中清理 | `[运行警告]` |

**各平台支持情况：**

| 平台 | 填写内容 | 额外配置 |
|------|----------|----------|
| 飞书文档 | 分享链接（`.feishu.cn/sheets/...` 或移动后的 `.feishu.cn/file/...`） | 需配置 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`（见下方） |
| 腾讯文档 | 分享链接（`docs.qq.com/d/...`） | 无，程序自动转换为导出链接 |
| 金山文档/WPS | 文档内「下载 → xlsx 格式」获取的直链 | 无 |
| Office Excel | 可直接访问的 xlsx 下载链接 | 无 |

**飞书文档配置步骤（一次性）：**

1. 访问 [open.feishu.cn](https://open.feishu.cn) → 创建企业自建应用
2. 进入「权限管理」→ 搜索并开启对应权限：
   - 链接含 `/sheets/`（原生电子表格）：`sheets:spreadsheet:readonly`
   - 链接含 `/file/`（文档移动后变为此格式）：`drive:drive:readonly`
3. 进入「版本管理与发布」→ 申请发布（审核通过后生效）
4. 打开飞书电子表格 → 右上角「分享」→ 添加该应用为协作者（或设为组织内可查看）
5. 将 App ID 和 App Secret 填入代码或设置为环境变量

---

## 设备配置文件格式

在线表格或本地 `device_config.xlsx` 须包含以下四个 Sheet：

### Sheet：设备配置

| 设备编号 | 客户名称 | 桶数 | 设备归属 |
|----------|----------|------|----------|
| TW001    | 中润     | 2    | 中润     |
| TW002    | 某客户A  | 1    | 客户A    |

> - `客户名称` 列仅供运维人员阅读，**代码不使用此列**，可留空。
> - **设备编号不可为空**；为空的行将跳过，并触发飞书告警。
> - **桶数、设备归属对非补录设备不可为空**；为空时触发飞书告警（补录设备豁免）。
> - 此 Sheet 为总表，应包含**所有受管设备**（正常设备 + 排除设备 + 计量设备 + 补录设备）。
>   程序每次运行时自动核对：`设备配置总数 = 数据库设备数 + 补录不在DB的设备数`，不符时飞书告警。
> - 非补录设备若在数据库中无匹配记录，将触发飞书告警提示编号可能有误。

### Sheet：排除客户设置

| 排除客户名称           | 排除客户ID | 排除设备编码（...）               | 备注                              |
|------------------------|------------|-----------------------------------|-----------------------------------|
| 中润                   | 100046     |                                   | 测试客户，整体排除                |
| 贰壹陆                 | 100049     |                                   | 测试客户，整体排除                |
| 浙江杰威               | 100066     | MO24032700700019                  | 已替换旧设备                      |
| 浙江联众汽车零部件有限公司 | 100079  | TW24011700700032,TW24011700700033 | 已替换旧设备（多台用英文逗号分隔）|

> - `排除客户名称`：仅供运维人员阅读，**代码不使用此列**。
> - `排除客户ID`：**必填**，使用数据库 `customer_id`，避免同名客户误排除；为空或重复时触发飞书告警。
> - `排除设备编码`：**为空** → 排除该客户所有设备；**非空** → 只排除指定编号（英文逗号分隔），其余设备正常显示。
> - 若配置的客户ID或设备编码在数据库中无匹配，程序会在日志中记录并推送飞书告警。

### Sheet：计量客户设置

| 计量客户名称               | 计量客户ID |
|----------------------------|------------|
| 浙江荣发动力股份有限公司   | 100058     |
| 台州永裕工业有限公司       | 100070     |

> - **计量客户ID 必填**，为空或重复时触发飞书告警。
> - 若配置的客户ID在数据库中无匹配设备，程序会在日志中记录并推送飞书告警。
> - 配置后，报表将自动新增"**计量客户单列**" Sheet，格式与主表一致，序号独立编排。

### Sheet：未录入系统设备

> 设备硬件已交付给客户、但尚未录入管理系统（数据库无记录）时使用。设备录入系统后，需删除对应行。

| 未录入系统设备客户名称(必填项) | 未录入系统设备编码(必填项) | 安装日期(可选项，格式示例：2026.03.13) | 安装地点(可选项) |
|-------------------------------|---------------------------|----------------------------------------|-----------------|
| 某客户                        | TW25010100300001          | 2026.01.15                             | 浙江杭州        |

> - **客户名称和设备编号均为必填**，为空或设备编号重复时跳过该行并触发飞书告警。
> - 补录设备将追加到报表主表末尾，`油品型号` 显示为"**设备未安装，数据库无数据**"，`网络同步` 显示 **disabled**（灰色）。
> - 若补录设备编号已在数据库中存在，程序自动将其归为正常设备处理，并推送飞书告警提示维护人员从此 Sheet 中删除该记录。

---

## 运行方式

### 本地运行

```bash
python main.py
```

启动后每天 08:00 自动触发（若 08:00 后启动，次日 08:00 再触发）。

### 服务器部署（Linux）

```bash
# 上传文件
scp main.py requirements.txt deploy_linux.sh user@server:/tmp/

# 一键部署为 systemd 服务（开机自启、崩溃自动重启）
ssh user@server
cd /tmp && chmod +x deploy_linux.sh && sudo ./deploy_linux.sh

# 常用运维命令
systemctl status  daily-report-robot   # 查看状态
journalctl -u     daily-report-robot -f # 实时日志
systemctl restart daily-report-robot   # 重启
```

### 服务器部署（Windows）

```bat
# 下载 NSSM（https://nssm.cc/download），将 nssm.exe 放到项目目录
# 以管理员身份运行
deploy_windows.bat
```

### 生产环境凭据注入（推荐）

避免密钥硬编码在代码中，通过环境变量注入：

```ini
# Linux systemd 服务文件 [Service] 段
Environment="DB_PASSWORD=your_password"
Environment="WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY"
Environment="FEISHU_APP_ID=cli_xxx"
Environment="FEISHU_APP_SECRET=xxx"
Environment="FEISHU_ALERT_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxx"
```

修改后执行 `systemctl daemon-reload && systemctl restart daily-report-robot` 生效。

---

## 数据流程

```
MySQL 数据库
    │
    │  一次 SQL 查询（t_device / t_customer / t_oil_type / t_device_oil）
    ▼
get_db_data()
    │  返回：device_code, customer_id, customer_name, avai_ratio,
    │        device_status, modify_time, oil_model, install_time, location
    ▼
process_data()
    ├─ 1. load_config()  ← 飞书API / 腾讯文档导出 / 本地 Excel 三路回退
    │       ├─ 设备配置：空编号告警、非补录设备桶数/归属为空告警
    │       ├─ 排除客户设置：客户ID空值/重复告警
    │       ├─ 计量客户设置：客户ID空值/重复告警
    │       └─ 未录入系统设备：必填项校验、设备编号重复告警
    ├─ 2. 过滤空客户名设备（SQL层已过滤，Python层双保险）
    │   2b. 设备配置 DB匹配校验（非补录设备编号不在DB则告警）
    │       + 总数核对（配置数 ≠ DB设备数+实际补录数则告警）
    ├─ 3. 按 exclusion_rules 过滤（基于 customer_id 精准匹配）
    │       + 排除规则DB匹配校验（未命中则告警）
    ├─ 4. 按安装时间升序排列
    ├─ 5. 补全桶数、设备归属字段
    ├─ 6. check_sync()  ← device_status + modify_time 判断在线/离线/停用
    ├─ 7. 数据清洗（空值填充）
    ├─ 8. 按 metered_ids 筛出计量客户子集 → df_metered
    │       + 计量客户ID DB匹配校验（未命中则告警）
    ├─ 9. 生成序号
    ├─ 10. 列重命名与筛选
    └─ 11. 追加补录设备（仅追加不在DB的编号；已入库则归为正常设备并告警提示清理）
    │
    ▼
generate_excel_with_format(df_final, filename, df_metered)
    ├── Sheet "安卓设备日统计表"  ← 全量设备（含计量客户）
    └── Sheet "计量客户单列"      ← 仅计量客户（df_metered 非空时写入）
    │
    ▼
send_to_robot()  ← 上传并推送至企业微信
```

---

## 网络在线状态判断逻辑

| 优先级 | 条件 | 状态值 | 报表颜色 |
|--------|------|--------|----------|
| 1 | `t_device.status == 1`（已停用） | `disabled` | 灰色 |
| 2 | `modify_time` 为空（从未上报数据） | `never_synced` | 紫色 |
| 3 | `modify_time` 距今 **> 72 小时** | `offline` | 红色 |
| 4 | `modify_time` 距今 **24 ~ 72 小时** | `offline_warn` | 橙色 |
| 5 | `modify_time` 距今 **≤ 24 小时** | `online` | 绿色 |

---

## Excel 报表说明

| Sheet 名称 | 内容 | 触发条件 |
|------------|------|----------|
| 安卓设备日统计表 | 全量符合筛选条件的设备（含计量客户） | 始终输出 |
| 计量客户单列 | 仅计量客户设备，序号独立编排 | 配置文件存在"计量客户设置" Sheet 且非空时自动生成 |

列顺序：`序号 | 客户名称 | 设备编号 | 油品型号 | 库存(%) | 桶数 | 设备归属 | 网络同步 | 安装时间 | 安装地点`

**UI 样式：**

| 元素 | 字体 | 字号 | 行高 | 对齐 | 背景色 |
|------|------|------|------|------|--------|
| 标题行 | 微软雅黑 | 16pt 加粗 | 45 | 居中 | #1F4E79 深蓝，白色文字，加粗底边框 |
| 表头行 | 微软雅黑 | 11pt 加粗 | 26 | 居中 | #2E75B6 中蓝，白色文字 |
| 数据行 | 微软雅黑 | 10pt | 20 | 见下 | 白色 / #F0F5FA 浅蓝交替 |

数据行对齐规则：
- **左对齐**：客户名称、设备编号、油品型号、安装地点
- **居中**：序号、库存(%)、桶数、设备归属、网络同步、安装时间

**库存(%) 条件格式：**

| 区间 | 颜色 |
|------|------|
| > 30% | 绿色 |
| 5% ~ 30% | 黄色 |
| ≤ 5% | 红色 |
| 无数据 | 橙色 |

**网络同步条件格式：**

| 值 | 颜色 | 含义 |
|----|------|------|
| online | 绿色 | 24h 内正常上报 |
| offline_warn | 橙色 | 24~72h 未上报，短期预警 |
| offline | 红色 | 超过 72h 未上报，长期失联 |
| never_synced | 紫色 | 从未上报，设备未激活 |
| disabled | 灰色 | 已停用 |

---

## 设计决策记录

### 配置来源：SQL 直查 vs API 调用

早期版本调用后端 REST API 获取数据，现改为直接 SQL 查询：

| 维度 | API 方案（旧） | SQL 方案（当前） |
|------|--------------|----------------|
| 架构复杂度 | 两步（DB + HTTP） | 一步（DB） |
| 可靠性 | 额外故障点（网络/服务宕机） | 更可靠 |
| 数据一致性 | 两次请求时间点不同 | 同一查询快照 |

### 在线配置表加载策略

| 平台 | 加载方式 | 原因 |
|------|----------|------|
| 飞书文档 | 飞书开放平台 API | 公开分享链接仅返回预览页，无法直接下载 |
| 腾讯文档 | 自动转换分享链接为 `/d/export/{id}?format=xlsx` | 导出接口公开可访问 |
| 金山文档/WPS | 用户手动获取直链 | 服务端返回人机验证页，无法自动下载 |
| 本地 Excel | 直接读取文件 | 在线均失败时的兜底方案 |

### 排除规则：从名称匹配升级为 ID 匹配

早期按客户名称字符串过滤，存在同名误排除和空格匹配失败问题。改为按 `customer_id` 精准匹配：

| 旧方案 | 当前方案 |
|--------|---------|
| 配置列：`排除客户`（名称字符串） | 配置列：`排除客户ID`（数据库 ID） |
| 同名客户会被误排除 | ID 唯一，不存在误排除 |
| 空格导致匹配静默失效 | ID 为数字，无空格问题 |

### 库存字段修正：avai_oil → avai_ratio

| 字段 | 含义 | 问题 |
|------|------|------|
| `avai_oil`（旧） | 剩余油量（升），如 609.40 | 列名显示`库存(%)`，阈值 5/30 对升数永远绿色 |
| `avai_ratio`（当前） | 剩余百分比（%），如 60.90 | 与阈值匹配，红黄绿配色正常生效 |
